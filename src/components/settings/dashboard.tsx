import { Button } from '@mui/material';
import React, { FC } from 'react';

const Dashboard: FC = () => {
  return (
    <div>
      <Button>abc</Button>
    </div>
  );
};

export default Dashboard;
