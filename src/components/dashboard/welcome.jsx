import './sass/welcome.sass';

const Welcome = ({ id }) => {
    return (
        <div id="welcome">
            <div id="join">
            </div>
            <div id="leave">
            </div>
        </div>
    );
};

export default Welcome;